@model List<ShipmentCarrierDto>

@{
    ViewData["Title"] = "Shipment Carriers List";
}



@section Styles{
    <!-- Plugin css for this page -->
    <link rel="stylesheet" href="~/admin/assets/vendors/datatables.net-bs4/dataTables.bootstrap4.css">
    <!-- End plugin css for this page -->
}

<div class="content-wrapper">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Shipment Carriers List</h4>
            @if(User.IsInRole("Admin"))
            {
                <a asp-area="Admin" asp-controller="ShipmentCarriers" asp-action="Create" class="btn btn-gradient-primary btn-rounded btn-fw" style="margin-bottom: 20px;">Add New Shipment Carrier</a>
            }
            <div class="row">
                <div class="col-12">
                    <table id="order-listing" class="table">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach(var item in Model)
                            {
                                <tr>
                                    <td>@item.CarrierName</td>
                                    <td>
                                        <a asp-area="Admin" asp-controller="ShipmentCarriers" asp-action="Update" asp-route-id="@item.Id" class="btn btn-outline-info btn-fw" style="margin-right: 25px;">Edit</a>
                                        <a asp-area="Admin" asp-controller="ShipmentCarriers" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger btn-fw" onclick="return confirm('Are you sure you want to delete the selected record?');">Delete</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- content-wrapper ends -->



@section Scripts{
    <!-- Plugin js for this page -->
    <script src="~/admin/assets/vendors/datatables.net/jquery.dataTables.js"></script>
    <script src="~/admin/assets/vendors/datatables.net-bs4/dataTables.bootstrap4.js"></script>
    <!-- End plugin js for this page -->

    <!-- Custom js for this page -->
    <script src="~/admin/assets/js/data-table.js"></script>
    <!-- End custom js for this page -->

    @if (TempData["Deleted Successfully"] is not null)
    {
        <script>
            if (@Convert.ToBoolean(TempData["Deleted Successfully"]).ToString().ToLower())
            {
                toastr.success("Shipment Carrier has been deleted successfully");
            }
            else
            {
                toastr.error("Unexpected error happened while trying to delete the Shipment Carrier.");
            }
        </script>
    }

    @if (TempData["Created Successfully"] is not null)
    {
        <script>
            if (@Convert.ToBoolean(TempData["Created Successfully"]).ToString().ToLower())
            {
                toastr.success("Shipment Carrier has been created and saved successfully");
            }
            else
            {
                toastr.error("Unexpected error happened while trying to insert/save the Shipment Carrier.");
            }
        </script>
    }

    @if (TempData["Error"] is not null)
    {
        <script>
            if (@Convert.ToBoolean(TempData["Error"]).ToString().ToLower())
            {
                toastr.error("Error While Processing the Request.");
            }
        </script>
    }
}
